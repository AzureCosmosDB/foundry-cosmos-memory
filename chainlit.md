# Cosmos Memory Chat

Chat with a prompt agent deployed in Microsoft Foundry Agent Service and give it durable, user-scoped memory in Azure Cosmos DB.

Choose a non-sensitive demo user ID. Teach the agent a preference, goal, or constraint, then start a new conversation to see that memory recalled without carrying over the old transcript. Switch users to verify memory isolation.
